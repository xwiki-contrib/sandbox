<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.xwiki.concerto</groupId>
    <artifactId>concerto-xwiki</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>
  <artifactId>xwiki-concerto-wiki</artifactId>
  <name>XWiki Concerto - Wiki</name>
  <packaging>xar</packaging>
  <description>The .xar used for the wiki shipped with Concerto</description>
  <dependencies>
    <dependency>
      <groupId>com.xpn.xwiki.products</groupId>
      <artifactId>xwiki-enterprise-wiki</artifactId>
      <version>${product.enterprise.version}</version>
      <type>xar</type>
    </dependency>
    <dependency>
      <groupId>org.xwiki.concerto</groupId>
      <artifactId>xwiki-application-xwootManager</artifactId>
      <version>${pom.version}</version>
      <type>xar</type>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>com.xpn.xwiki.platform.tools</groupId>
        <artifactId>xwiki-xar-plugin</artifactId>
        <executions>
          <execution>
            <phase>process-resources</phase>
            <id>expand-parent-xar</id>
            <goals>
              <goal>unxar</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <groupId>com.xpn.xwiki.products</groupId>
          <artifactId>xwiki-enterprise-wiki</artifactId>
          <outputDirectory>${project.build.directory}/original/</outputDirectory>
        </configuration>
      </plugin>
      <!-- Register the XWoot management panel in XWikiPreferences -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>xml-maven-plugin</artifactId>
        <executions>
          <execution>
            <phase>compile</phase>
            <id>register-xwoot-panel</id>
            <goals>
              <goal>transform</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <transformationSets>
            <transformationSet>
              <dir>${project.build.directory}/original/XWiki/</dir>
              <outputDir>${project.build.outputDirectory}/XWiki</outputDir>
              <includes>
                <include>XWikiPreferences.xml</include>
              </includes>
              <stylesheet>src/main/build/transformation/addPanel.xsl</stylesheet>
            </transformationSet>
          </transformationSets>
        </configuration>
      </plugin>
   </plugins>
  </build>
</project>
